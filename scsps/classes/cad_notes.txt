CAD Changes
cad_lookup_class  New layout


cad->generate($sku, $cad_code, $options)
Drop link 
options:
generate_sku (default: $database->registry->cad->generate_sku)
prj (array item/varset) (not required if generate_sku)
generate (0-Download, 1-Email) Default: download
staging (default 0)
debug (curl.txt)


results is object
cad_link
curl_count
transaction_id
